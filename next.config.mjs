/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "images.pexels.com", // if you're using Pexels too
    ],
  },
};

export default nextConfig;
